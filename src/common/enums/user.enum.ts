export enum EUserActive {
  ACTIVE = 0,
  UNACTIVE = 1,
  LOCKED = 2,
}

export enum EUserType {
  PERSONAL = 'PERSONAL',
  VENDOR = 'VENDOR',
}

export enum EUserGender {
  MALE = 0,
  FEMALE = 1,
  OTHER = -1,
}
